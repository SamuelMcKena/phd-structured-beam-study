# Legacy Reference-Kernel Snapshots

The publication study does not import these files at runtime.  I copied them
here on 2026-05-29 so the study folder contains the historical sources that fed
the earlier scalar and modular-lab notebooks:

- ``balyian_shared_kernel_v4.py`` from ``Full Code/``
- ``balyian_shared_kernel_v4_full.py`` from ``Full Code/``; this was only a
  wrapper around the active v4 kernel, but I keep it so legacy notebook
  references resolve inside the study snapshot.
- ``research_lab_shared_legacy.py`` from ``PhD_Modular_Lab/``

The active scalar engine is ``Publication_Study/bessel_twin_core.py``.  The
active categorized formula home is ``Publication_Study/vbb_study/equations``.
