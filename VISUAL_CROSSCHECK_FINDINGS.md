# Stage 6X Visual Cross-Check Findings

Tolerance used where a same-plane numeric counterpart exists: relative error
<= 1 percent or absolute error <= 0.02 um for radius-like quantities. Phase
mask comparisons use active-aperture wrapped-phase RMS; a same-mask comparison
should be near zero.

Rows are sorted with `DIVERGES` first.

| Classification | Source / function | Visual implication and plane | Validated counterpart | Numbers | Notebooks affected |
|---|---|---|---|---|---|
| DIVERGES | `vbb_study/publication/quicklook.py::plot_slm_preview` via `run_slm_phase_preview()` | SLM wrapped phase mask; SLM plane. | Same quicklook config passed through `bt.run_case(..., path="realistic")`, comparing `result["slm_field"]["phase"]`. | Preview uses sample-config design: `gamma_slm_deg=0.242851`, cone `2.05956 lp/mm`. Run-case air-path design: `gamma_slm_deg=0.592539`, cone `5.02533 lp/mm`. Active-aperture wrapped-phase RMS delta `1.84141 rad`; `96.65%` of active pixels differ by >0.1 rad. Rebuilding the phase with `vbb_studies.beam_air_config(twin)` gives RMS `0.0`. | quicklook/00 cell 10; same `_phase_from_config()` also feeds four-condition phase masks. |
| DIVERGES | lab/04 inline carrier-cone diagram | First-order carrier vs axicon cone achievability; SLM/free-space Fourier plane. | `bt.run_case(..., path="realistic")` first-order metrics for the same regime/blaze. | Notebook cone values use sample-medium design: general `2.05956 lp/mm`, limits `9.26803 lp/mm`. Run-case metrics use air-path design: general `5.02533 lp/mm`, limits `22.6140 lp/mm`. Boolean disagreements: general blaze 32 px is plotted valid but run-case invalid; limits blaze 12 px is plotted valid but run-case invalid. | lab_realism/04 cells 8-9. Proposed one-line fix for follow-up: compute this diagram from `vbb_studies.beam_air_config(cfg)` or from run-case first-order metrics. |
| DIFFERENT_PLANE | `vbb_study/vbb_train_viz.py::plot_train_visualiser` | Train-stage final panels imply a transverse feature radius at z=0: holographic "focused surface seed" or physical "after physical axicon". | Run-case `volume["planes"]["peak"]` at the measured peak z. Propagating the train field to that z with the same BL-ASM engine reproduces the metric. | Current fast/general displayed 192-px crop: holographic seed ring `1.38758 um` at z=0 vs run-case peak ring `1.63542 um` at z=`-50.3309 um`; propagated-to-peak ring `1.63542 um`. Physical post-axicon ring `4.64934 um` at z=0 vs peak ring `2.38750 um` at z=`75.6798 um`; propagated-to-peak ring `2.38750 um`. | lab_realism/01, lab_realism/02, lab_realism/03. |
| AGREES | `vbb_study/publication/quicklook.py::plot_material_proxy_preview` | Peak-plane material fluence/threshold proxy; in-medium peak XY plane. | `SampleResult.metrics["peak_fluence_J_cm2"] / incubated_threshold_J_cm2` from the same through-sample result. | `proxy_max_ratio=1.64562`; metrics ratio `1.64562`; delta `4.4e-16`. | quicklook/00 cell 20. |
| AGREES | `vbb_study/vbb_sample_study.py::plot_sample_result_comparison` | Through-sample XY peak plane and XZ ring/core-radius overlay. | `SampleResult.metrics["ring_radius_um"]` at `volume_result["peak_index"]`. | General holographic uncorrected check: metrics ring `4.66347 um`; per-z overlay at peak `4.65462 um`; delta `-0.00885 um` (`0.19%`); peak z `487.5 um`. | lab_realism/05. |
| UNVERIFIABLE | `vbb_study/publication/quicklook.py::plot_ideal_preview` when fed `run_ideal_beam_preview()` | Direct true Bessel-Gauss target preview; peak plane from a custom propagated target field. | No locked run-case counterpart for this "ideal_target" source model. Closest run-case conical route is a different source model. | Ring agrees with conical in this check (`2.53996 um` vs `2.53996 um`), but axial zone differs (`86.8705 um` vs conical `113.759 um`) and peak z differs (`0 um` vs `75.9375 um`). | quicklook/00 cell 12. |
| UNVERIFIABLE | `vbb_study/publication/quicklook.py::plot_through_sample_preview` | In-medium XZ preview from surface; z measured from sample surface. | `run_case(study_kind="through_sample")` is not implemented; the counterpart is `vbb_sample_study.run_through_sample()` only. | No independent run-case plane exists for this preview. | quicklook/00 cell 18. |
| UNVERIFIABLE | `vbb_study/publication/quicklook.py::plot_four_condition_phase_comparison(panel="phase_masks")` | Explanatory phase masks for four phase states; SLM plane. | The current notebook phase-mask panel is paired with ideal/conical plot cases, which do not carry a run-case SLM phase. | No same-route validated phase mask exists for the ideal/conical panel. Note: the helper shares the quicklook `_phase_from_config()` divergence above if treated as a lab-realistic mask. | quicklook/00 cell 27. |
| UNVERIFIABLE | `Publication_Study/publication_diagnostics.py::plot_self_healing_diagnostics` | Obstacle-plane and post-obstacle recovery fields. | No run-case metric covers an obstructed beam propagated from an internal obstacle plane. | Representative bundle: obstacle z `52.5 um`, peak recovery end `1.00907`, peak recovery max `1.03533`; no run-case counterpart. | scalar/03 cells 11 and 16. |
| UNVERIFIABLE | scalar/03 inline recovery curves | Peak/on-axis recovery after custom obstacles. | No run-case obstructed-beam counterpart. | Same self-healing campaign data as above; diagnostic-only recovery ratios. | scalar/03 cell 13. |
| UNVERIFIABLE | scalar/05 inline SAS validation montage and radial profiles | BL-ASM vs SAS propagated example fields. | These are validation examples, not study cases with locked run-case metrics. | Conical axicon example at z=`120 um`: BL/SAS normalized-intensity relative L2 `0.149163`, SAS retained power `0.993865`, full-power ratio `1.0`. | scalar/05 cells 9-10. |
| UNVERIFIABLE | `vbb_study/vbb_train_viz.py::plot_sampling_qa` | Sampling margin map over target core/zone. | No run-case volume plane; the plot is an analytic validity-map diagnostic. | No plane/radius counterpart. | lab_realism/01, lab_realism/02, lab_realism/03. |
| UNVERIFIABLE | lab/04 inline pupil-fill schematic | Gaussian 1/e2 footprint versus objective pupil; geometry only. | No run-case visual/volume counterpart. | Geometry-only; no z-plane. | lab_realism/04 cell 7. |
| UNVERIFIABLE | lab/06 inline `plot_journey_grid` | Stitched full air-to-sample XZ journey; air z<0, sample z>0. | No locked run-case counterpart for the stitched journey volume. | Sample sub-results have their own metrics, but the stitched air/sample coordinate frame is not locked as a run-case quantity. | lab_realism/06 cells 7-8. |
| UNVERIFIABLE | `vbb_study/vbb_vector.py::plot_analyzer_family_grid` | Ideal vector analyzer maps; sample-plane Jones target. | Scalar `run_case()` has no vector analyzer counterpart. | No scalar run-case plane or metric exists. | vector/01 cell 7. |
| UNVERIFIABLE | `vbb_study/vbb_vector.py::plot_polarization_quiver` | Ideal vector polarization quiver. | Scalar `run_case()` has no polarization-orientation counterpart. | No scalar run-case plane or metric exists. | vector/01 cell 7. |
| UNVERIFIABLE | `vbb_study/vbb_vector.py::plot_total_and_analyzer_panel` | Current-lab/paper-replica vector analyzer maps. | Scalar `run_case()` has no vector analyzer counterpart. | No scalar run-case plane or metric exists. | vector/02 cell 7. |

